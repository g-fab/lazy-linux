#!/bin/bash

# Configuration automatique du thème XFCE moderne

# Créer la config XFCE par défaut pour tous les nouveaux utilisateurs
mkdir -p /etc/skel/.config/xfce4/xfconf/xfce-perchannel-xml

# Thème GTK : Arc-Dark
cat > /etc/skel/.config/xfce4/xfconf/xfce-perchannel-xml/xsettings.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<channel name="xsettings" version="1.0">
  <property name="Net" type="empty">
    <property name="ThemeName" type="string" value="Arc-Dark"/>
    <property name="IconThemeName" type="string" value="Papirus-Dark"/>
  </property>
  <property name="Gtk" type="empty">
    <property name="FontName" type="string" value="Fira Code 10"/>
  </property>
</channel>
EOF

# Fond d'écran personnalisé (wallpaper)
cat > /etc/skel/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-desktop.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<channel name="xfce4-desktop" version="1.0">
  <property name="backdrop" type="empty">
    <property name="screen0" type="empty">
      <property name="monitor0" type="empty">
        <property name="image-style" type="int" value="5"/>
        <property name="rgba1" type="array">
          <value type="double" value="0.2"/>
          <value type="double" value="0.2"/>
          <value type="double" value="0.2"/>
          <value type="double" value="1.0"/>
        </property>
      </property>
    </property>
  </property>
</channel>
EOF

chmod +x /etc/skel/.config/xfce4/xfconf/xfce-perchannel-xml/*.xml
